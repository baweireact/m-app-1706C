<!--components/book-nav/book-nav.wxml-->
<view class="m-nav">
  <text wx:for="{{navList}}" wx:key="id" class="m-nav-item {{currentId === item.id ? 'active' : ''}}" bind:tap="handleNav" data-ids="{{item.id}}" mark:id="{{item.id}}">{{item.title}}</text>
</view>
